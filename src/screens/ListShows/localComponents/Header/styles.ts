import { StyleSheet } from "react-native";
import { primary_gray } from "../../../../constants";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    backgroundColor: primary_gray,
    alignItems: "center",
  },
  image: { width: 140, height: 50, marginBottom: 20 },
});
